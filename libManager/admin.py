from django.contrib import admin
from libManager import models

# Register your models here.

# for model_ in libManager.models:
#     admin.site.register(model_)
# admin.site.register([models.Book, models.Author, models.PublishHouse])
# admin.site.register([models.Book, models.Albumn, models.MotionPicture])
